const controller = require('../controllers/user');

exports.api = (router) => {
    // router.route('/user/v1/resetPassword').post(controller.resetPassword);
};

exports.authApi = (router) => {
    // router.route('/user/v1/changePassword').post(controller.changePassword);
    
};
